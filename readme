Задание: Напишите функцию которая удаляет из любого валидного js файла все комментарии, без ущерба исходному коду.

1 Создал файл comments.js, который состоит из валидного js кода, а так же однострочных и блочных комментариев.

2 В файле index.js реализовал две js функции removeBlockComments и removeLineComments. Эти функции принимали на вход строку - разбивали ее на символы и циклом переберая каждый символ проверяли на совпадение символа блочному или строчному комментарию. Обнаружив начало комментария заменяли остальные символы на пустую строку.

3 Для чтения js файлов я использовал модуль fs из Node js

4 Для проверки работы функции в корне проэкта в терменале достаточно ввести "node index.js"
